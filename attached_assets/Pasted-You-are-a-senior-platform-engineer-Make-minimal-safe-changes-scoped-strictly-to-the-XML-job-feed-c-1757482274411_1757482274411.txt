You are a senior platform engineer. Make minimal, safe changes scoped strictly to the XML job feed component. Preserve existing app behavior elsewhere.

# Context
Repo/Repl: <your link>
Stack: Flask, APScheduler, SQLAlchemy, SendGrid, OpenAI (GPT-4o), lxml, SFTP
Current State: Disorganized XML generator; tearsheet-driven monitors trigger rebuilds & SFTP uploads; RapidChangeTracker alerts
Constraints: No unrelated refactors; no secrets in code
Non-negotiables:
- Only rebuild XML component
- AI-generated <assignedrecruiter>, <jobfunction>, <jobindustries> remain AI-generated
- All other data from Bullhorn API, except <apply_email> and <url> from config
- SFTP publish to site root as `myticas-job-feed-v2.xml` (public at https://myticas.com/myticas-job-feed-v2.xml)
- Update docs/runbooks

# Goal
Remove legacy XML, pause automation, and rebuild the feed to match the live template at https://myticas.com/myticas-job-feed-v2.xml.
Maintain tearsheet-driven updates (the five live tearsheets): add/remove/modify must trigger rebuild and upload.

# Steps
1) Freeze: add XML_FEED_FRZ; disable XML-related APScheduler jobs & uploads; route alerts to XML_ALERTS_EMAIL
2) Remove legacy XML builders/templates/routes; keep Bullhorn, AI utils, SFTP, SendGrid
3) Implement feeds/myticas_v2.py:
   - build_myticas_feed(): Bullhorn→XML; add the 3 AI fields; set <apply_email> from APPLY_EMAIL and <url> from PUBLIC_JOB_URL_BASE
   - validate_myticas_feed(xml): fetch live template, derive golden structure; check node order/presence & basic types; deterministic bytes
   - publish(xml): write + SFTP upload to site root as `myticas-job-feed-v2.xml` when unfrozen
4) Tearsheet flow: export the five current tearsheets; disable during rebuild; re-create and re-enable after validation; rebuild→validate→(if unfrozen) publish; debounce; backoff on rate limits
5) Config/Secrets: XML_FEED_FRZ, APPLY_EMAIL, PUBLIC_JOB_URL_BASE, SFTP_HOST/SFTP_USER/SFTP_KEY(/SFTP_PORT), XML_ALERTS_EMAIL (+ reuse Bullhorn creds)
6) Tests: mapping + AI injection; snapshot for 2–3 jobs; CLI scripts/validate_feed.py
7) Docs: schema outline; Bullhorn→XML mapping; tearsheet-change flow; freeze/unfreeze; exact publish location; validation & ops commands
8) Unfreeze staging first (alerts to you), then production

# Acceptance Criteria
- Output structurally matches the live template and is published at https://myticas.com/myticas-job-feed-v2.xml
- Data source rules honored (Bullhorn vs AI vs config)
- Legacy XML removed
- Feature flag cleanly toggles monitors & uploads
- Tests pass; deterministic snapshot
- Docs updated

# Change Boundaries
Allowed: feeds/, scheduler wiring, monitoring toggles, SFTP publish, docs, tests, scripts
Forbidden: unrelated routes/UI/auth; DB schema changes unless absolutely required (ask first); removing AI classification utilities

# Required Deliverables
- Validation Report
- Unified Diffs
- Commands Run
- Summary of Fixes

# Execution Hints
Setup: pip install -r requirements.txt
Run: flask --app app run --host 0.0.0.0 --port 8000
Validate: python scripts/validate_feed.py --limit 5 --out /tmp/myticas-job-feed-v2.xml
Freeze/Unfreeze: set XML_FEED_FRZ in Secrets