- [Schema rollout convention](schema-rollout.md) — Alembic is NOT the live boot path; column/index adds must go in the model + seeding/migrations.py idempotent ALTERs + an Alembic migration for parity.
- [candidate_profile_embedding size](embedding-table-size.md) — its ~180MB is real live data (~6.7k rows of TEXT embeddings in TOAST), not bloat; the "150 rows" was never-analyzed stale stats.
- [Prod read-replica verification](prod-readonly-verification.md) — catalog facts (columns/indexes/reloptions) + row data replicate; pg_stat_user_tables counters are replica-local and read never/0, don't alarm on them.
- [Legacy .doc extraction in prod](doc-extraction-prod.md) — antiword EIOs in prod; .doc must use the pure-Python olefile parser (runs first), antiword only a fallback. Don't reintroduce it as primary.
- [Screening prompt storage](screening-prompt-storage.md) — live global screening/clearance instructions are the DB row `VettingConfig.global_custom_requirements`; the .txt file is only a seed, so file edits don't change prod.
- [Screening audit feature flags](screening-audit-flags.md) — audit/cutover flags live in the SECRET store (not env vars); agent can't read/set values, user must flip + republish for prod to pick up.
- [Telemetry gap on module disable](telemetry-gap-on-module-disable.md) — a known-active call site writing zero openai_call_log rows points to the write path/stale deploy, not pricing; re-enable+republish restored it.
- [Phase-2 AI-detection pilot](phase2-ai-detection-pilot.md) — external AI-resume-detection deferred; detectors are weak on short resume/bullet text, so pilot (GPTZero preferred) before any scoring weight.
- [Fraud third-party composite gating](fraud-third-party-composite.md) — the "third-party submission" composite needs a PRESENT, valid, non-personal, non-disposable email; never treat missing/personal/disposable as qualifying.
- [Inbound parse-failure noise gate](inbound-parse-failure-noise.md) — inbound webhook treats every email as a candidate; blank-sender/subject + no-attachment + no-info is noise → record 'ignored', never fire the admin alert.
- [Inbound LB body truncation](inbound-lb-body-truncation.md) — prod LB truncates inbound POST bodies to 0/4096 bytes (Content-Length intact); systematic not intermittent, retries don't help, needs Replit infra fix or O365 Graph pull.
- [Cheaper-model scoring experiment](cheaper-model-scoring-experiment.md) — route clear-non-matches to gpt-4.1-mini via "cheap-first + escalation" (never trust mini directly); shared SHADOW_LOGGING_DISABLED killswitch gates embedding shadow too.
- [Mini prose/null numeric fields](mini-numeric-field-coercion.md) — gpt-4.1-mini emits prose/null where gpt-5.4 gave numbers; coerce defensively, skip-gate on unparseable (never →0, avoids Enforce false-reject); watch `AI analysis error` count, not just false-negative lines.
- [Scoring cost-cutover eval](scoring-cutover-eval.md) — schema diet + L2 cache layout BOTH fail; gpt-5.4 scoring has ~7pt inherent variance, prompt-cache already ~85%, format micro-opts not worth it.
- [Screening re-vet redundancy](screening-revet-redundancy.md) — ~28% of scoring calls re-score the same (candidate, job); self-screen cooldown doesn't cover the inbound/auditor/scheduled re-vet paths. Use match→vetting_log to measure (telemetry can't).
- [Mailbox-pull ingestion contingency](mailbox-pull-contingency.md) — LB truncates inbound POSTs; pull applicants from apply@ via Graph into existing process_email. Dedupe on message_id (fallback to Graph id); contiguous-prefix cursor; nextLink backfill.
- [Telemetry dollar gap = stale pricing](telemetry-billing-gap.md) — /admin/ai-cost under-reports $ because PRICING rates lag OpenAI (not hidden usage); token counts are accurate. Derive real rates = dash category $ ÷ our token sums.
- [Apply-page source attribution](apply-source-attribution.md) — ?source= is a hardcoded LinkedIn default in every apply URL; the GET referrer is the only true channel signal. Resolve referrer>utm>param, stamp into "has applied on {source}" email → inbound carries it to Bullhorn.
- [Bullhorn candidate base size](bullhorn-candidate-base-size.md) — real ATS Candidate base is ~900k+ (≈984k via search/Candidate, matches UI badge); an earlier "~9k" note was WRONG. Per-candidate sweeps MUST be scoped.
- [Fraud resume-reuse vs duplicates](fraud-resume-reuse-duplicates.md) — same-person duplicate record (same name+email) is NOT fraud → 0-point "consider merging" line; only a different identity scores. PG must use DISTINCT ON (not GROUP BY+MAX) for coherent name/email/date.
- [Inbound body short-read](inbound-body-short-read.md) — webhook body_len≈4096 << content_length = WSGI short read (truncates résumé/None-None), NOT boundary/raw-MIME; fix = loop-read full body.
- [Start Fresh net-new cutover](start-fresh-net-new-cutover.md) — net-new re-enable needs BOTH floors: vetting_cutoff_date (inbound) AND last_run_timestamp (Bullhorn detectors, hard floor/no clamp, freezes while vetting off); Start Fresh now moves both.
- [Duplicate pipeline entries intentional](duplicate-pipeline-entries-intentional.md) — same candidate ID appearing N× in a job Pipeline is duplicate JobSubmissions (wanted: shows repeat applies), NOT a dedup bug; only duplicate candidate RECORDS matter.
- [Multi-tenant query scoping](multitenant-scoping.md) — env-scope every bullhorn_job_id/candidate_id read AND write; scope_query (routes) / _job_req_for (services) no-op at <=1 env to preserve single-tenant parity; non-default creds fail closed.
- [Per-brand screening profiles](per-brand-screening-profiles.md) — env override map + profile gate the screening config; Myticas-unchanged invariant relies on default-env→{}/'standard' and NEVER editing the standard prompt literal (swap post-render).
- [Resume vs inbound name paths](resume-name-paths.md) — résumé header heuristic must reuse shared split_full_name/is_valid_name/NAME_PARTICLES; lowercase-particle surnames (el-/van/de) once dropped → grabbed "Career Highlights". Casing now title-cased (McDonald→Mcdonald tradeoff).
